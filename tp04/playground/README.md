# playground/

Vos scripts hors Nest, pour les cartes 1 à 3. `exemple.ts` est un point de départ :

```sh
npx tsx playground/exemple.ts
cp playground/exemple.ts playground/mon-script.ts
```

Première ligne de chaque script : `import 'dotenv/config';` pour lire `JWT_SECRET`.
